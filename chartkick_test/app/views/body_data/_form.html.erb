<%= form_with(model: body_datum, local: true) do |form| %>
  <% if body_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(body_datum.errors.count, "error") %> prohibited this body_datum from being saved:</h2>

      <ul>
      <% body_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight %>
  </div>

  <div class="field">
    <%= form.label :fat %>
    <%= form.text_field :fat %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
